"""Vendored SimpleMem text backend for MemBase.

This package vendors the text-only SimpleMem pipeline and adapts it to run
inside MemBase with instance-scoped settings instead of a process-global
``config`` module.
"""

